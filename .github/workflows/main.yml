name: first workflow
on: 
   push:
     branches:
       - main

jobs:
 example-job:
   runs-on: self-hosted

   steps:
     - name: print a welcome message
       run: echo "welcome to our  first github actions workfolow!"
   
       
   
